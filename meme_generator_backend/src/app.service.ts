import { Injectable } from '@nestjs/common';

@Injectable()
export class AppService {
  // App service can be used for application-wide business logic
  // Currently no specific functionality needed
}
